using Microsoft.VisualStudio.TestTools.UnitTesting;
using Moq;
using NUnit.Framework;
using Umbraco.Core.Services.Implement;
using uSlack.EventHandlers;
using uSlack.EventHandlers.Components;

namespace uSlack.Tests
{
    [TestFixture]
    class ContentEvents
    {
        [Test]
        void Test()
        {
            //var handlers = new Mock<IContentHandlers>();

            //PrivateType eventOriginType = new PrivateType(typeof(ContentService));
            //eventOriginType.InvokeStatic("Published", null, null);

            ////Assert.IsTrue(overthinkingTheProblemIsDangerous);


            //var EventsComponent = new ContentServiceEvents(handlers.Object);
        }
    }
}
